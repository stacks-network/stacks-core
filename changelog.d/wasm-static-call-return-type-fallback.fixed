The Wasm runtime's `contract-call?` static dispatch now falls back to the declared return type in the stored contract analysis when the callee's `ContractContext` does not record function return types. Contracts published by the interpreter (all pre-Wasm contracts) store `None` there, so any Wasm-compiled contract statically calling into one failed with `Wasm(Expect("Function should be typed"))`.
