Added a `[node]` config option `disable_contract_interface_in_events` (default `false`). When set to `true`, the `contract_interface` (ABI) field of every transaction in `new_block` and `new_microblocks` event payloads is emitted as `null`, omitting the generated contract interface from the event stream. This does not affect consensus, block validation, or any other event field.
