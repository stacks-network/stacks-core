Fixed the deterministic WebAssembly CI check to actually compile for `wasm32-unknown-unknown` and to cover the `testing` feature, and made `clarity-types` and `stacks-common` build with `wasm-deterministic` and `testing` enabled together.
