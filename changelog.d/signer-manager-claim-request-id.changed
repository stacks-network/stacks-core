In the reference `signer-manager.clar` contract, `claim-staker-rewards` now returns `{ earned, withdrawal-request-id }` instead of a bare `uint`; where `withdrawal-request-id` is `(some id)` when the payout was routed to an L1 sBTC withdrawal and `none` for an sBTC payout.
