Instead of silently ignoring old block proposals, reject them with the new `ProposalTooOld` reason. This allows the miner to break out of its `propose_block` loop and mine a new block instead of being stuck in a live lock until the next Bitcoin block arrives.
